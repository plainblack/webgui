To run the benchmark test:

Install and run memcached

Install the following CPAN modules:

*) CHI
*) CHI::Driver::Memcached
*) CHI::Driver::DBI

cd /data/WebGUI/sbin
perl benchCache.pl --configFile=<config>

